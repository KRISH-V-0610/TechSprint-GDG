{
  "name": "shushrut",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"frontend,backend,python\" --prefix-colors \"cyan,green,yellow\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"cd PYTHON && python main.py\"",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
